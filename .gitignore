node_modules/
work/
ready-to-upload/
archive/
renders/
snapshots/
*.log
.env
.env.local
.last-run
.hyperframes/
.worktrees/

# Internal development notes — handoffs, planning, design specs.
# Kept locally for reference; not part of the public OSS surface.
docs/

# Local convenience symlinks into .agents/skills/ (not portable on Windows without core.symlinks).
# The canonical skill content lives at .agents/skills/ and IS committed.
.claude/skills/gsap/
.claude/skills/hyperframes/
.claude/skills/hyperframes-cli/
.claude/skills/hyperframes-registry/
.claude/skills/website-to-hyperframes/

# Skill eval workspaces — regeneratable; re-run grade_runs.mjs + build_benchmark.mjs to rebuild.
.claude/skills/*-workspace/
.claude/skills/review.html
.claude/skills/combined_benchmark.json

# Per-user Claude Code overrides
.claude/settings.local.json
